# Backend runtime configuration
# Copy these values into your shell environment or a local .env loader.
API_HOST=127.0.0.1
API_PORT=8000
# Set this in production. Local loopback requests work without it.
API_TOKEN=
# Safe default. Use 'live' only after the demo-account rollout checklist passes.
TRADING_MODE=paper

# Required for the AI execution loop. Keep the real key only in server/.env (never commit).
AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_TIMEOUT_SECONDS=30
# MetaTrader 5 credentials: provided dynamically via frontend login at runtime.
# Never commit real values. Leave empty for production; frontend supplies per-session.
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
# Optional: full path to terminal64.exe for auto-launch (Windows)
MT5_TERMINAL_PATH=
# Linux only: opt in after configuring and starting the mt5linux bridge.
MT5LINUX_ENABLED=0


NEWS_API_KEY=
# Optional NewsAPI.org (or compatible) key; Yahoo RSS remains keyless fallback.
NEWS_API_URL=https://newsapi.org/v2/everything
YAHOO_FINANCE_RSS_URL=https://feeds.finance.yahoo.com/rss/2.0/headline?s=EURUSD%3DX%2CGBPUSD%3DX%2CGC%3DF&region=US&lang=en-US
LOG_LEVEL=INFO

# Risk limits are set in server/config.py. Review them before live trading.
# Peak-drawdown halts are latched until an authenticated manual reset.
