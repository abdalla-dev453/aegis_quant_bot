# Local default. Replace with the public reverse-proxy origin for deployment.
VITE_API_BASE=http://localhost:8000

# Must match server/API_TOKEN when the backend token is enabled.
# This value is bundled into the frontend; use TLS and a private deployment.
VITE_API_TOKEN=
